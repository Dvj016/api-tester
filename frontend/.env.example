# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Note: ElevenLabs API key is now stored securely on the backend
# The frontend uses a backend proxy endpoint for voice generation
# No need to add NEXT_PUBLIC_ELEVENLABS_API_KEY here anymore